<% wrap_layout :application do %>
  <h1><%= current_article.title %></h1>

  <%= yield %>

  <p class="meta">
    Posted
    <%= link_to current_article.date.strftime("%B %e, %Y at %l:%M %P"), current_article.url %>
  </p>
<% end %>
